package model;

public class Enums {
    public enum PaperFormat { A3, A4 }
    public enum ColorMode { COLOR, GRAYSCALE }
    public enum PrintQuality { DRAFT, STANDARD, HIGH }
    public enum Orientation { PORTRAIT, LANDSCAPE }
    public enum Alignment { TOP, BOTTOM, LEFT, RIGHT }
}
